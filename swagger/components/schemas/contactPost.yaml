type: object
required:
  - userId
  - name
  - phoneNumber
  - contactType
properties:
  $ref: './contactCommonProperties.yaml'
  photo:
    description: "Contact's photo"
    type: string
    format: binary
